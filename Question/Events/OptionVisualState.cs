namespace VRQuestion
{
    // 选项视觉状态 - 由QuestionPanel驱动OptionButton切换
    public enum OptionVisualState
    {
        Normal,
        Hover,
        Selected,
        Correct,
        Wrong,
        Missed,
        Disabled
    }
}
